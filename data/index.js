const expenses = require('./expenses');
const expensePayments = require('./expensePayments');
const approvals = require('./approvals');
const corporates = require('./corporates');

module.exports = {
  expenses,
  expensePayments,
  approvals,
  corporates,
};
