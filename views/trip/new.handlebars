<div class="new-post">
  <form id="trip-form" class="new-post-form">
    <div class="row">
      <div class="col-25">
        <label>
          Trip name
        </label>
      </div>
      <div class="col-75">
        <input
          id="trip-name"
          type="text"
          name="name"
          placeholder="Name"
          value="{{trip.name}}"
          class="full-width-input"
        />
        <div id="trip-name-error" hidden></div>
      </div>
    </div>
    <div class="row">
      <div class="col-25">
        <label>
          Description of the trip
        </label>
      </div>
      <div class="col-75">
        <input
          id="trip-description"
          type="text"
          name="Description"
          placeholder="description"
          value="{{trip.description}}"
          class="full-width-input"
        />
        <div id="trip-description-error" hidden></div>
      </div>
    </div>
    <div class="row">
      <div class="col-25">
        <label>
          Start time
        </label>
      </div>
      <div class="col-75">
        <input
          id="trip-start-time"
          type="date"
          name="start-time"
          placeholder="start time"
          class="full-width-input"
          value="{{trip.startTime}}"
        />
        <div id="trip-start-time-error" hidden></div>
      </div>
    </div>
    <div class="row">
      <div class="col-25">
        <label>
          End time
        </label>
      </div>
      <div class="col-75">
        <input
          id="trip-end-time"
          type="date"
          name="end-time"
          placeholder="end time"
          class="full-width-input"
          value="{{trip.endTime}}"
        />
        <div id="trip-end-time-error" hidden></div>
      </div>
    </div>
    <div class="row">
      <div class="col-25">
        <label for="manager">
          Manager
        </label>
      </div>
      <div class="col-75">
        <select id="trip-manager" class="full-width-input" name="managerId">
          <option value="">
            Select manager...
          </option>
          {{#each users}}
            <option value="{{_id}}">
              {{name}}
              (
              {{email}}
              )
            </option>
          {{/each}}
        </select>
        <div id="trip-manager-error" hidden></div>
      </div>
    </div>
    <div class="row">
      <input hidden id="trip-employees" name="trip-employees" value="" />
      <div class="col-25">
        <label for="trip-employee-selection">
          Employees
        </label>
      </div>
      <div class="col-75">
        <select id="trip-employee-selection">
          <option value="">
            Select employee...
          </option>
          {{#each users}}
            <option value="{{_id}}">
              {{name}}
              (
              {{email}}
              )
            </option>
          {{/each}}
        </select>
      </div>
    </div>
    <button class="submit" disabled id="trip-employee-add">
      Add employee
    </button>
    <div>
      <h4>
        Employees in Trip
      </h4>
      <ul id="trip-employee-selection-preview"></ul>
      <div id="trip-employees-error" hidden></div>
    </div>
    <div>
      <input type="submit" value="Create New Trip" />
    </div>
    <div>
      <input type="reset" value="Clear" />
    </div>
    <div id="trip-submit-error" hidden></div>
  </form>
  <a href="/trip">
    back to Trips
  </a>
</div>
<script defer src="/js/trip-new.js"></script>